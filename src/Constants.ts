const PORT: number = 8095;
const LOCAL_URL: string = `http://localhost:${PORT}`;
const CLOUD_URL: string = `https://ec2-54-195-42-42.eu-west-1.compute.amazonaws.com:${PORT}`;

export const SERVICE_URL = CLOUD_URL;